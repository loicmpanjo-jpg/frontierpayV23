services:
  - type: web
    name: afm-api-gateway
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api_gateway.main:app --host 0.0.0.0 --port $PORT
databases:
  - name: afm-postgres
    plan: free
